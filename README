Daelib
======

Daelib is a library of common, program independent utilities.


Notes
-----

* Includes an configurable assertion system.
* Includes a global logger, dlog.
* The logger is used by various containers.
* *_init() and *_kill() do memory management.

